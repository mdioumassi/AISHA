{% extends '@Inscription/layout.html.twig' %}

{% block content %}
    <div class="w3-container">

    <table class="w3-table-all w3-card-4">
        <thead>
        <tr class="w3-green">
            <th>Nom</th><th>Prenom</th><th>Genre</th><th>Date Naissance</th><th>Parent</th><th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for enfant in enfants %}
            <tr>
                <td>{{ enfant.nom }}</td>
                <td>{{ enfant.prenom }}</td>
                <td>{{ enfant.genre }}</td>
                <td>{{ enfant.dateNaissance|date('d/m/Y') }}</td>
                <td><span class="w3-badge w3-yellow "><a href="#"> {{ enfant.parent.prenom }} {{ enfant.parent.nom|upper }}</a></span></td></a>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}